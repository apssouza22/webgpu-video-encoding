Place sample media here:

- `video.mp4` — base video clip with audio (used for picture and sound)
- `overlay.png` — transparent PNG shown as overlay from t = 2s

The app checks for these files on startup and shows an error if they are missing.
