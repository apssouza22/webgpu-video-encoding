Place sample media here:

- `video.mp4` — first video clip, ideally with audio
- `video-2.mp4` — second video clip, ideally with audio
- `overlay.png` — transparent PNG shown on the right side from t = 1s to 4s
- `overlay-2.png` — transparent PNG shown on the left side from t = 1s to 4s

The app checks for these files on startup and shows an error if they are missing.
